// Como podemos rodar isso em um arquivo .ts sem causar erros? 

interface empregado {code:number,name:string};

let employee:empregado = {
    code : 10,
    name : "John"
}

console.log(employee.code)
console.log(employee.name)
